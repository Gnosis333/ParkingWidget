# 오늘 몇 층에 주차했더라?
### BLE 기반 주차층 자동 감지 위젯 — 실생활 문제를 코드로 해결하기

---

## 1. 문제 (아이디어의 출발점)

- 아파트 지하주차장 B1 / B2, 매일 다른 층에 주차
- 엘리베이터에서 내리는 순간: **"어제 몇 층에 댔지?"**
- 사진 찍기, 메모하기 → 매번 까먹음. 습관이 안 됨
- 목표: **아무것도 하지 않아도** 홈 화면에 주차층이 떠 있게 하자

---

## 2. v1 — 수동 위젯 (첫 번째 버전)

- 홈 화면 위젯에 **B1 / B2 버튼 두 개**, 주차 후 한 번 탭
- 기술: `AppWidgetProvider` + `RemoteViews` + `SharedPreferences`
- 단일 클래스, 하루 만에 완성
- 한계: 결국 "탭하는 것"도 까먹음 → **완전 자동화 필요**

---

## 3. v2 아이디어 — 폰이 알아서 층을 감지하게

- 핵심 관찰: 지하주차장에는 층마다 **BLE(블루투스 저전력) 기기**들이 있다
  - 아파트 원패스 도어 비콘, 조명 컨트롤러, 키박스 등
- 가설: 주변 BLE 신호를 스캔하면 **"지금 내가 어느 층인지"** 알 수 있다
- 폰이 백그라운드에서 스캔 → 층 판정 → 위젯 자동 갱신

---

## 4. 아키텍처 (완성형)

```
BleMonitorService (포그라운드 서비스, 상시 BLE 스캔)
        │  앵커 신호 수신
        ▼
ParkingAnchors (층 판정 로직 단일 출처)
        │  "B1" / "B2" 판정
        ▼
ParkingWidgetProvider (홈 화면 위젯 갱신)

+ WatchdogReceiver  : AlarmManager 15분 워치독 (서비스 사망 시 부활)
+ BootReceiver      : 재부팅 시 자동 시작
+ MainActivity      : BLE 디버그 콘솔 (실측 로그 수집용)
+ MonitorLog        : 파일 로그 (사후 진단의 핵심 무기)
```

**기술 스택**: Kotlin · Android BLE Scan API · Foreground Service · AlarmManager · RemoteViews · SharedPreferences

---

## 5. 첫 번째 좌절 — "비콘으로는 층을 구분할 수 없다"

계획: 층마다 설치된 도어 비콘(iBeacon)의 ID로 층 구분

현장 실측 결과 (반복 측정):

| 기대 | 현실 |
|------|------|
| 층마다 다른 비콘 ID | B1·B2 비콘의 UUID/major/minor **완전 동일** |
| MAC 주소로 구분 | MAC이 **30~40분마다 회전** (보안 기능) |
| 안정적인 신호 | 문 앞 -64dBm, 조금 멀어지면 소실 |

→ **비콘 방식 폐기.** 대신 발상 전환:

> "회전하지 않는 **고정 MAC 기기**(조명 컨트롤러, 키박스 등)를 층별 '앵커'로 삼자"

- 최근 20초 내 수신된 앵커 중 **더 강한 신호의 층**으로 판정
- 실측 검증: 반대층 앵커 0건, 층 분리 깨끗하게 성공

---

## 6. 진짜 싸움 — "백그라운드에서 조용히 죽는 앱" 4차전

집에서는 잘 되는데 실사용에서 계속 미감지. **에러 없이 조용히 실패**하는 것이 특징.
파일 로그(monitor_log.txt)를 심어 증거를 모으며 4번에 걸쳐 원인을 좁혀 나감.

| 라운드 | 증상 | 진범 | 해법 |
|--------|------|------|------|
| 1차 | 아침마다 위젯 멈춤, 앱 강제종료하면 복구 | OS가 프로세스는 살려두고 **스캔 콜백만 무력화** | 자가치유 루프 + 스캔 실패 백오프 재시작 |
| 2차 | 그래도 재발 | **Doze 모드**에서 타이머(Handler) 시계가 멈춤 | AlarmManager 워치독 (Doze에서도 발화, 죽은 프로세스 부활) |
| 3차 | 서비스는 24시간 생존인데 앵커 수신 0건 | `ScanFilter.setDeviceAddress()`가 **랜덤 MAC 주소타입 기기를 영원히 못 잡음** (API 함정) | 이름 필터 병행 + 화면 켜짐 순간 버스트 스캔 |
| 4차 | 그래도 재발, 무필터 스캔조차 "기기 0종" | 위치 권한이 "앱 사용 중에만" → **백그라운드 BLE 결과를 OS가 조용히 차단** | 백그라운드 위치 권한 ("항상 허용") |

**핵심 교훈 — Android 백그라운드 BLE 4종 세트** (하나라도 빠지면 조용히 실패):

1. 위치 권한 "항상 허용"
2. 배터리 최적화 예외 (+삼성 "잠자지 않는 앱")
3. MAC 필터의 주소타입 함정 회피 (이름 필터 병행)
4. Doze 대응 워치독 (AlarmManager)

---

## 7. 디버깅 방법론 — 감이 아니라 로그로

- **파일 로그 우선**: 서비스 생명주기·스캔 시작/실패·워치독 발화·층 판정을 전부 기록 → "마지막 로그 시각"으로 사망 시점 특정
- **원격 진단**: `adb`로 화면 끄기/켜기 이벤트를 강제 발화시켜 버스트 스캔 재현, `cmd appops`로 권한 차단 시각(rejectTime)까지 확인 → 4차 진범 확정의 결정타
- **현장 실측**: 주차장 3지점 수동 스캔으로 앵커 발굴, RSSI 문턱값(-85dBm) 도입으로 층간 신호 누설 차단
- AI 페어 프로그래밍: **Claude Code**와 함께 로그 분석 → 가설 수립 → 코드 수정 → 실차 검증 사이클 반복

---

## 8. 결과

- **2026-07-12 최종 검증 성공**: 화면을 켜지 않은 채 엘리베이터로 B2→B1 이동, 두 층 모두 자동 감지·위젯 갱신
- 주차 후 **아무 조작 없이** 홈 화면에 현재 주차층 표시 — 최초 목표 달성
- 이후에도 실사용 로그 기반으로 커버리지 지속 보강 (새 앵커 발굴, RSSI 문턱 도입)

---

## 9. 배운 것 (발표 마무리)

- **작게 시작하기**: 버튼 2개짜리 수동 위젯 → 쓰면서 불편한 지점이 다음 버전을 정의
- **현실은 문서와 다르다**: 비콘 스펙만 믿었다면 실패. 현장 실측이 설계를 바꿈
- **조용한 실패가 가장 어렵다**: 에러 로그가 없을 때는 증거(파일 로그)를 직접 심어야 한다
- **환경 제약도 설계 대상**: OS 절전 정책, 권한 모델, 제조사 커스텀까지가 "요구사항"
- 실생활의 사소한 불편 하나가 BLE·백그라운드 서비스·전원 관리·원격 진단까지 이어지는 **훌륭한 학습 프로젝트**가 됨
